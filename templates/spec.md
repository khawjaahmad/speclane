# Spec delta: <Capability>

> A spec **delta** describes what this change adds, modifies, or removes relative to the current
> system. At archive time these merge into `specs/`. For a brand-new `feature`, write the full
> capability spec here instead of a delta.

## ADDED
<New behavior this change introduces. Use requirement + scenario form.>

### Requirement: <name>
The system SHALL <behavior>.

#### Scenario: <name>
- GIVEN <precondition>
- WHEN <action>
- THEN <expected observable result>

## MODIFIED
<Existing behavior this change alters. State the before → after.>

## REMOVED
<Behavior this change deletes, and the migration/cleanup implication.>

## Out of scope
<Behavior explicitly not changed, to prevent scope drift.>
