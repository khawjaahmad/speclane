---
description: Decompose a SpecLane change into verifiable tasks. Args: <slug>
---

You are running the **Tasks** stage of SpecLane for change `$ARGUMENTS`.

1. Read the change's `proposal.md`, `spec.md`, and `design.md`.
2. Fill `tasks.md` from `templates/tasks.md`. Each task is small, names what it changes, and names how it is verified. Keep tasks independently reviewable.
3. Write a test plan that maps to the acceptance criteria, sized to the track's risk.
4. Do not implement yet. Confirm the task list covers every acceptance criterion before stopping.
