# Prompt: SpecLane — Plan

You are running the **Plan** stage. Read the change's `proposal.md`, `spec.md`,
`sdd/constitution.md`, and `templates/design.md`.

Write `design.md`: approach, contracts touched (before → after, backward-compat), data &
migration, sequencing, rollback, alternatives. Flag approval-domain impact explicitly. Skip for
`quickfix`. Do not implement.
