# Prompt: SpecLane — Implement

You are running the **Implement** stage. Read `tasks.md`, `design.md`, `sdd/constitution.md`.

Implement one task at a time; write tests for changed behavior with each task. Respect contracts
exactly as recorded in `design.md` — if a contract change is needed, stop and surface it. Stay
within the proposal's scope. Summarize what a human must review.
