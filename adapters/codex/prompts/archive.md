# Prompt: SpecLane — Archive

You are running the **Archive** stage. Confirm all acceptance criteria are met in
`evidence-bundle.md`. Merge the `spec.md` delta into the matching capability under `specs/`,
capture any reusable lesson, then move the folder to `changes/archive/` (or instruct the user to
run `./scripts/speclane archive <slug>`).
