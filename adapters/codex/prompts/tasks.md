# Prompt: SpecLane — Tasks

You are running the **Tasks** stage. Read `proposal.md`, `spec.md`, `design.md`, and
`templates/tasks.md`.

Write `tasks.md`: small, independently verifiable tasks, each naming what it changes and how it
is verified, plus a test plan mapped to the acceptance criteria. Do not implement yet.
