---
mode: agent
description: Run the SpecLane Specify stage for a change slug.
---
Run the **Specify** stage of SpecLane. Read `sdd/constitution.md`, `docs/tracks.md`, and
`templates/proposal.md`/`templates/spec.md`. Choose a track, write `proposal.md` with observable
acceptance criteria, and (for feature/change) the `spec.md` delta. Write no implementation code;
list open questions for the human.
