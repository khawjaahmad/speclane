---
mode: agent
description: Run the SpecLane Implement stage for a change slug.
---
Run the **Implement** stage of SpecLane. Read `tasks.md`, `design.md`, `sdd/constitution.md`.
Implement one task at a time with tests for changed behavior. Respect contracts in `design.md`;
if a contract change is needed, stop and surface it. Stay within the proposal's scope.
